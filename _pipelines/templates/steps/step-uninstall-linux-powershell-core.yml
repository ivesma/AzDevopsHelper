#parameters:
steps:
  - task: Bash@3
    inputs:
        targetType: 'inline'
        script: |
          # Uninstall PowerShell
          apt-get remove -y powershell
        failOnStderr: true
    displayName: 'Uninstall PowerShell Core'
